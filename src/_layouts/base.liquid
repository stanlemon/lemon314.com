<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{ title }}</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body{% if bodyClass %} class="{{ bodyClass }}"{% endif %}>
{{ content }}
</body>
</html>
